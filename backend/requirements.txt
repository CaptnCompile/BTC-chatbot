# BTC Chatbot — backend dependencies
# Install into a virtual environment:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# LLM + agent graph
langchain-anthropic>=1.0
langgraph>=1.0
langchain-core>=1.0

# Market data feed (async HTTP)
httpx>=0.27

# Config
python-dotenv>=1.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23
